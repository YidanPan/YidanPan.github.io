# 题目：
给你一个只包含 `'('` 和 `')'` 的字符串，找出最长有效（格式正确且连续）括号 子串 的长度。

左右括号匹配，即每个左括号都有对应的右括号将其闭合的字符串是格式正确的，比如 `"(()())"`

# 要点解析：
- 打破固有思维 这个stack不用来存括号这种字符，反而是来存储括号的下标，方便后续计算统计需要用到多少括号
- stack第一个存-1（非常巧妙的一个技巧 因为最后这个数组下标直接相减就是子串大小 ）
- 